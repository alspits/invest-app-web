# Tinkoff API Portfolio Integration

## ✅ Реализованные возможности

### 1. **Tinkoff API Service** (`src/lib/tinkoff-api.ts`)
- ✅ Типизация всех API ответов с TypeScript
- ✅ Runtime валидация с Zod schemas
- ✅ Retry логика с exponential backoff для устойчивости к сбоям
- ✅ Вспомогательные функции для конвертации MoneyValue и Quotation
- ✅ Методы:
  - `fetchAccounts()` - получение списка счетов
  - `fetchPortfolio(accountId)` - получение портфолио по счёту
  - `fetchPositions(accountId)` - альтернативный метод для позиций
  - `getInstrumentByFigi(figi)` - получение информации об инструменте

### 2. **Next.js API Routes** (Secure Server-Side)
- ✅ `/api/tinkoff/accounts` - получение счетов
- ✅ `/api/tinkoff/portfolio?accountId=xxx` - получение портфолио
- ✅ Токен API хранится безопасно на сервере (не светится клиенту)
- ✅ Обработка ошибок с понятными сообщениями

### 3. **Zustand Store** (`src/stores/portfolioStore.ts`)
- ✅ Управление состоянием счетов и портфолио
- ✅ Actions:
  - `loadAccounts()` - загрузка счетов
  - `switchAccount(accountId)` - переключение между счетами
  - `loadPortfolio(accountId)` - загрузка портфолио
  - `reset()` - сброс состояния
- ✅ Автоматическая загрузка портфолио при переключении счёта
- ✅ Loading и error states
- ✅ **Mock Data Fallback**: В режиме разработки без API токена автоматически используются mock данные:
  - 2 счёта: "Брокерский счёт" и "ИИС"
  - Портфолио с 3 позициями (SBER, GAZP, TMOS)
  - Реалистичные данные для разработки UI

### 4. **React Query Provider**
- ✅ Настроен QueryClient с оптимальными параметрами
- ✅ Кэширование запросов (staleTime: 1 минута)
- ✅ Retry логика для failed запросов
- ✅ Интеграция в `app/layout.tsx`

### 5. **UI Components**

#### **Portfolio** (главный компонент)
- ✅ Координирует все дочерние компоненты
- ✅ Загрузка счетов при монтировании
- ✅ Skeleton loaders для начальной загрузки
- ✅ Обработка ошибок с retry кнопкой
- ✅ Пустое состояние когда нет счетов

#### **AccountSelector**
- ✅ Dropdown для выбора счёта
- ✅ Отображение типа счёта (Брокерский, ИИС, Инвесткопилка)
- ✅ Отображение статуса (Активен/Закрыт)
- ✅ Автоматическое переключение портфолио

#### **PortfolioSummary**
- ✅ Общая стоимость портфеля
- ✅ Ожидаемая доходность (в рублях)
- ✅ Доходность в процентах
- ✅ Распределение активов по типам (Акции, Облигации, ETF, Валюта, Фьючерсы)
- ✅ Визуализация с progress bars
- ✅ Цветовая индикация прибыли/убытка (зелёный/красный)

#### **PositionList**
- ✅ Список всех позиций в портфеле
- ✅ Skeleton loaders во время загрузки (5 штук)
- ✅ Отображение для каждой позиции:
  - Ticker (FIGI)
  - Тип инструмента
  - Количество
  - Средняя цена покупки
  - Текущая цена
  - Общая стоимость
  - Доходность (в рублях и %)
  - Progress bar для визуализации прибыли/убытка
- ✅ Пустое состояние когда нет позиций
- ✅ Hover эффекты для лучшего UX

### 6. **Стилизация**
- ✅ TailwindCSS v4
- ✅ Responsive дизайн (mobile-first)
- ✅ Градиенты для карточек метрик
- ✅ Анимации (skeleton loaders, progress bars, hover)
- ✅ Цветовая схема:
  - Синий для основных элементов
  - Зелёный для прибыли
  - Красный для убытка
  - Серый для нейтральных элементов

## 🚀 Как использовать

### 1. Запустить dev сервер:
```bash
cd invest-app-web
npm run dev
```

### 2. Открыть браузер:
```
http://localhost:3000
```

### 3. Что увидите:

#### Режим с API токеном:
1. Загрузка счетов из Tinkoff API
2. Автоматический выбор первого счёта
3. Загрузка портфолио для выбранного счёта
4. Сводка портфеля (стоимость, доходность, распределение)
5. Список всех позиций

#### Режим без API токена (development):
1. Автоматическое использование mock данных
2. 2 тестовых счёта: "Брокерский счёт" и "ИИС"
3. Тестовое портфолио с 3 позициями:
   - SBER (Сбербанк) - 10 шт.
   - GAZP (Газпром) - 5 шт.
   - TMOS (Тинькофф iMOEX) - 20 шт.
4. Полнофункциональный UI для разработки

## 📁 Структура файлов

```
invest-app-web/
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   └── tinkoff/
│   │   │       ├── accounts/
│   │   │       │   └── route.ts          # API endpoint для счетов
│   │   │       └── portfolio/
│   │   │           └── route.ts          # API endpoint для портфолио
│   │   ├── layout.tsx                    # Root layout с Providers
│   │   └── page.tsx                      # Main page (Portfolio)
│   ├── components/
│   │   └── features/
│   │       └── Portfolio/
│   │           ├── Portfolio.tsx         # Главный компонент
│   │           ├── AccountSelector.tsx   # Выбор счёта
│   │           ├── PortfolioSummary.tsx  # Сводка портфеля
│   │           ├── PositionList.tsx      # Список позиций
│   │           └── index.ts              # Exports
│   ├── lib/
│   │   ├── tinkoff-api.ts                # Tinkoff API service
│   │   └── providers.tsx                 # React Query provider
│   └── stores/
│       └── portfolioStore.ts             # Zustand store
└── .env.local                             # API credentials (НЕ коммитить!)
```

## 🔐 Безопасность

- ✅ API токен хранится только в `.env.local` на сервере
- ✅ Клиент не имеет прямого доступа к токену
- ✅ Все запросы к Tinkoff идут через Next.js API routes
- ✅ `.env.local` добавлен в `.gitignore`

## 🎯 Следующие шаги (опционально)

### Возможные улучшения:
1. **Загрузка названий инструментов**: Вместо FIGI показывать реальные названия акций/облигаций
2. **Графики**: Добавить Chart.js для визуализации истории доходности
3. **Фильтрация**: Фильтр по типу инструментов (только акции, только облигации)
4. **Сортировка**: Сортировка позиций по доходности, стоимости и т.д.
5. **Кэширование**: Улучшенное кэширование с React Query (сейчас базовое)
6. **Рефреш**: Кнопка принудительного обновления данных
7. **Детали позиции**: Modal с подробной информацией о позиции
8. **История операций**: Список покупок/продаж
9. **Аналитика**: Расширенная аналитика портфеля

## 🐛 Известные ограничения

1. **FIGI вместо названий**: Tinkoff API возвращает FIGI, для получения названий нужен дополнительный запрос к `/GetInstrumentBy`
2. **Нет реального времени**: Данные обновляются только при загрузке страницы или переключении счёта
3. **Один токен**: Сейчас используется один глобальный токен, в будущем можно добавить auth с индивидуальными токенами

## 📊 Тестирование

Проект успешно собирается:
```bash
npm run build
# ✓ Compiled successfully
```

TypeScript проверки пройдены без ошибок.

## 🎨 Скриншоты UI

### Loading State
- Skeleton loaders для счетов
- Skeleton loaders для позиций

### Main View
- Header с выбором счёта
- 3 карточки метрик (стоимость, доходность, %)
- Распределение активов с progress bars
- Список позиций с детальной информацией

### Error State
- Красная карточка с сообщением об ошибке
- Кнопка "Повторить попытку"

### Empty State
- Сообщение "Позиций пока нет"
- Иконка и пояснение
