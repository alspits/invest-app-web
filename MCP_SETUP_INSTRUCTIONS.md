# Инструкция по настройке MCP серверов

## ✅ Что уже сделано:

1. **Исправлены все конфигурации MCP серверов**
2. **Обновлены конфиги** в `.mcp.json` и `~/.claude.json`
3. **Docker образы уже загружены** (context7, brightdata)
4. **Все серверы проверены и готовы к работе**

## 🔧 Серверы, которые ГОТОВЫ к работе:

Эти серверы уже настроены и работают:

- ✅ **filesystem** - доступ к файлам проекта
- ✅ **sequential-thinking** - улучшенное мышление
- ✅ **memory** - память между сессиями
- ✅ **memory-bank** - управление памятью проекта
- ✅ **context7** - контекстный поиск (токен уже добавлен)
- ✅ **brightdata** - веб-скрейпинг (токен уже добавлен)

## 🔑 Серверы, требующие токены:

### 1. Brave Search (поисковая система)

**Где получить API ключ:**
- Зарегистрируйтесь на https://brave.com/search/api/
- Бесплатный план: 2,000 запросов/месяц

**Как добавить:**
Откройте `.mcp.json` и замените:
```json
"BRAVE_API_KEY": "YOUR_BRAVE_API_KEY_HERE"
```
на:
```json
"BRAVE_API_KEY": "ваш_реальный_ключ"
```

### 2. Context7 (контекстный поиск) - ✅ УЖЕ НАСТРОЕН

**Статус:** Токен уже добавлен в конфигурацию
**API Key:** ctx7sk-f67eda6b-194a-4f14-a82f-f2fb4aa0c196

✅ Этот сервер готов к использованию!

### 3. BrightData (веб-скрейпинг) - ✅ УЖЕ НАСТРОЕН

**Статус:** Токен уже добавлен в конфигурацию

✅ Этот сервер готов к использованию!

## 📝 Пошаговая инструкция для Brave Search:

1. **Получите API ключ:**
   - Перейдите на https://brave.com/search/api/
   - Зарегистрируйтесь (бесплатный план: 2,000 запросов/месяц)
   - Скопируйте ваш API ключ

2. **Откройте файл конфигурации:**
   ```bash
   code /Users/aleksandrspitsin/Documents/webapp/invest-app-web/.mcp.json
   ```

3. **Замените плейсхолдер:**
   Найдите строку:
   ```json
   "BRAVE_API_KEY": "YOUR_BRAVE_API_KEY_HERE"
   ```

   Замените на:
   ```json
   "BRAVE_API_KEY": "ваш-реальный-ключ"
   ```

4. **Сохраните файл** (Cmd+S)

5. **Перезапустите Claude Code:**
   - В VSCode: Cmd+Shift+P → "Developer: Reload Window"
   - В терминале: Выйдите и запустите Claude Code снова

## 🧪 Проверка работы:

**Уже работают:**
- ✅ filesystem (доступ к файлам)
- ✅ sequential-thinking (улучшенное мышление)
- ✅ memory (сессионная память)
- ✅ memory-bank (память проекта)
- ✅ context7 (контекстный поиск)
- ✅ brightdata (веб-скрейпинг)

**После добавления API ключа:**
- ⚠️ brave-search (веб-поиск) - добавьте ключ

## ⚠️ Важно:

- **Не коммитьте `.mcp.json` с реальными токенами в git!**
- Файл `.mcp.json` уже добавлен в `.gitignore`
- Храните токены в безопасности

## 🐳 Docker требования:

- Docker Desktop должен быть запущен
- Образы уже загружены: `mcp/context7`, `acuvity/mcp-server-brightdata`
- Если Docker не запущен, серверы context7 и brightdata не будут работать

## 📚 Документация:

- MCP Protocol: https://modelcontextprotocol.io
- Brave Search API: https://brave.com/search/api/docs
- Context7: https://context7.ai/docs
- BrightData: https://docs.brightdata.com

## 🆘 Если что-то не работает:

1. Проверьте, что Docker Desktop запущен
2. Проверьте правильность токенов в `.mcp.json`
3. Перезагрузите окно VSCode
4. Проверьте логи в панели "Output" → "MCP Servers"
